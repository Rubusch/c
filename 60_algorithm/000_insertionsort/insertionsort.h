// insertionsort.h
/*
//*/

#ifndef INSERTIONSORT
#define INSERTIONSORT

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define max(a, b) ((a) > (b) ? (a) : (b))
void sort(int *first, int size);

#endif
